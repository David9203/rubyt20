<p id="notice"><%= notice %></p>

<h1 class="cnt">Posts</h1>

<table>
  <thead>
    <tr>

      <th>Band</th>
      <th>Album</th>
      <th>Genere</th>
      <th>Comments</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>

      <tr>
        <td><%= post.band %></td>
        <td><%= post.album %></td>
        <td><%= post.genere %></td>
        <td><%= post.comments %></td>

        <td><%= link_to 'Show', post %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>        
       <td><%= image_tag post.avatar.url(:thumb, timestamp:false)%></td> 
      </tr>
      
      

    
  </div>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Post', new_post_path %>
